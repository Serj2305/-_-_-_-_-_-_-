version: "3.9"

services:
  
  django:
    build: .
    command: sh -c 'python3 manage.py makemigrations; python3 manage.py migrate; python3 manage.py runserver 0.0.0.0:80'
    container_name: studysignsproject-django
    ports:
      - "80:80"
    env_file:
      - ./.env